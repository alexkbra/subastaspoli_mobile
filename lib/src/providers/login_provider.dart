
//import 'dart:convert';

//import 'package:hometechclaim_mobile/src/models/user_model.dart';
//import 'package:http/http.dart' as http;

class LoginProvider {

  //bool _cargando = false;

  //Future<UserModel> _procesarRespuesta(Uri url) async {
  //  final resp = await http.get('http://192.168.39.126:8090/hometechclaim/login?page=0&size=2&sort=imagen');
  //  final decodedData = json.decode(resp.body);
  //}

  //Future<UserModel> login(String email, String password) async {
    //if (_cargando) return null;
    //_cargando = true;

    /*final url = Uri.http(Utils.url, 'login', {
        'email': email,
        'password': password
      });*/

    //final resp = await _procesarRespuesta(null);


  //}

}